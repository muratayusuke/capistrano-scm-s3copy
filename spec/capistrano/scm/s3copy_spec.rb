require 'spec_helper'

describe Capistrano::Scm::S3copy do
  it 'has a version number' do
    expect(Capistrano::Scm::S3copy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
